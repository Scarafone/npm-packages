const ynjs = require('./index')
const { dele } = require('@scarafone/files-helper')

describe('YNJS Test Suite', () => {

    it('should import package as function', () => {
        expect(typeof ynjs).toBe('object')
    })

    it('should execute init command', () => {

    })

})